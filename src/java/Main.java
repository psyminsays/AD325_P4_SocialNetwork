

/**
 * Main class to test the Profile class functionality.
 * This class creates a Profile object, sets its fields, and prints the details of the profile using the getter methods.
 */
public class Main {

    /**
     * The entry point of the program. It demonstrates the usage of the Profile class.
     * A Profile object is created, and its details are printed to the console using getter methods.
     *
     * @param args command-line arguments (not used in this example)
     */
    public static void main(String[] args) {
    }
}